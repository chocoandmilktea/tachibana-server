# tachibana-server

立花証券e支店APIにログインし、Railway上で常駐するサーバー。フロントエンド側のリポジトリ
`daytrade-simulator`（Vercel）と対で動く。

## このファイルの位置づけ

- `README.md` は人間向けの入口。このサーバーが何をするか・どのファイルが何を担うか・
  環境変数の一覧は README を読むこと。このファイルには複製しない
- `CLAUDE.md`（このファイル）は実装時の前提。コードを読んだだけでは分からない
  「壊してはいけない理由」だけを書く
- 運用情報（デプロイしてよい時間帯・ログの確認手順）は当リポジトリではなく
  `daytrade-simulator` の `docs/OPERATIONS.md` にある

## 参照の書き方

- 行番号は書かない。コード上の値・設定は識別子名で参照する（`grep` で追える形にする）
- 別リポジトリ `daytrade-simulator` の行番号は書かない。ファイル名までにとどめる
- 環境変数の「値」は書かない。変数名と用途のみ
- 本文は日本語。全体で100行程度に収める

## premarketLogger.js の壊してはいけない前提

以下は「直すと壊れる」箇所。理由の詳細は該当関数・定数の直上コメントに残してある。

### 変更前にコメントを読むこと（要点のみ）

- `warn()` の内部は `console.log`。`console.warn` に戻さない
  — 詳細は該当関数の直上コメント
- `running` の解除は `.finally()` のみ。`.then()` や `catch` の中に移さない
  — 詳細は該当関数の直上コメント
- `lastSessionDate` によるガードは維持する。`running` とは役割が異なり、片方では代替できない
  — 詳細は該当関数の直上コメント
- 1ティックにつき立花へのPOSTがチャンク数ぶん発生する。分割の単位は `CHUNK_SIZE`
  （既定は `DEFAULT_CHUNK_SIZE`）で、要求回数は対象銘柄数をこれで割った数になる。
  必ず直列で投げる（並列化すると通番が入れ替わって弾かれる）
  — 詳細は `fetchOnce()` の直上とループ内のコメント
- POSTサイズの閾値は `POST_SIZE_WARN_KB` / `POST_SIZE_ERROR_KB` / `POST_SIZE_LIMIT_KB` の3段階。
  ログを出し分けるのは前の2つで、`POST_SIZE_LIMIT_KB` はVercelの上限としてメッセージに表示するだけ。
  `error()` の内部は `console.error` のまま維持する
  — 詳細は各定数の直上コメント
- 立花の `market-price` は1要求につき先頭120件までしか返さず、121件目以降は黙って捨てられる。
  **この上限自体は解消していない。** チャンク分割はこれを回避するための実装であって、
  上限が無くなったわけではない — 詳細はファイル冒頭の方針コメント
- 予想生成の呼び出し（`postPrediction`）は、生ログの保存が成功したときだけ行う。
  失敗しても収集の成否には影響させない（例外を外へ投げない）
  — 詳細は `postPrediction()` の直上コメント

### 理由まで理解してから触ること

- **`errorCount` と `tickErrorCount` は別物。数え方を揃えてはいけない。**
  `errorCount` は収集終了時に `records` から導出する非加算のカウンタ（全チャンクが失敗して
  エラーレコードになったティックの数）。`tickErrorCount` はセッション内で加算していくカウンタで、
  分割後はチャンク単位で増える。片方を他方の数え方に揃えると、収集終了のログに並ぶ2つの数字が
  同じ意味になってしまい、「何ティック中の何チャンクが落ちたのか」という障害の切り分けができなくなる

- **`PREMARKET_CODES` のパースは `toUpperCase()` のうえ `/^[0-9A-Z]{4}$/` で判定する。**
  数字4桁に限定したり `parseInt` / `Number` / `isNaN` による数値判定へ変えたりすると、
  `278A` のような英字入りの新形式コードが落ちる。この形を崩さないこと

- **`MAX_CODES` と `CHUNK_SIZE` はモジュール読み込み時に一度だけ確定する。**
  トップレベルで `parsePremarketMax()` / `parsePremarketChunkSize()` を呼んで代入しているため、
  Railwayの環境変数を変えただけでは反映されない。反映には再起動が必要

- **収集窓は `START_MINUTE` と `END_MINUTE` で平日 8:45〜9:06 の21分のみ。**
  この窓の外では日時判定しか走らず、立花へのアクセスも収集も一切起きない。
  そのため変更の失敗はその場では分からず、翌営業日の朝まで検知できない。
  **デプロイ後の確認は、翌朝のログを見るまで完了しない。** その場で正常に見えても確認済みとしないこと

## 銘柄リストの決まり方

- 収集対象は `PREMARKET_CODES` からのみ組み立てる（`parsePremarketCodes()` → `RECEIVED_CODES` →
  `MAX_CODES` で先頭から切って `CODES`）。並び順がそのまま優先順になる
- `daytrade-simulator` 側のRedisキー `scan:universe` とは連動していない。
  当リポジトリに `scan:universe` を参照するコードは存在しない。
  自動スキャンの対象銘柄と寄り前収集の対象銘柄は別系統であり、片方を変えても他方は変わらない
- **実際に何件を対象にしているかはコードからは判別できない。**
  `DEFAULT_CODES` は `PREMARKET_CODES` が未設定・全件不正のときに使うフォールバック用の最小値であって、
  実運用の件数ではない。実際の件数はRailwayの環境変数側にあるため、
  コードだけを見て「対象は◯件」と断定しないこと
